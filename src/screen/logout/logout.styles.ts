import { StyleSheet } from 'react-native';
import { colors } from '../../utils/colors';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1,
  },
  message: {
    fontSize: 25,
    textAlign: 'center',
    color: colors.almostBlcak,
    marginBottom: 25,
    width: '80%',
  },
});
